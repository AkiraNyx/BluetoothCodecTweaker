<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="BluetoothCodecTweaker.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:BluetoothCodecTweaker.Helpers"
    xmlns:vm="using:BluetoothCodecTweaker.ViewModels"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <helpers:InfoBarSeverityConverter x:Key="InfoBarSeverityConverter" />
        <helpers:InfoBarVisibilityConverter x:Key="InfoBarVisibilityConverter" />
        <helpers:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
        <helpers:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <helpers:ConnectionStatusToGlyphConverter x:Key="ConnectionGlyphConverter" />
        <helpers:SupportStatusConverter x:Key="SupportStatusConverter" />
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel
            Padding="36,24,36,36"
            Spacing="16"
            MaxWidth="800">

            <!-- Header -->
            <TextBlock
                Text="蓝牙音频编码管理"
                Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock
                Text="选择蓝牙音频设备并配置音频编码（Codec）"
                Style="{StaticResource BodyTextBlockStyle}"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

            <!-- Info Bar -->
            <InfoBar
                x:Name="StatusInfoBar"
                Title="{x:Bind ViewModel.InfoBarTitle, Mode=OneWay}"
                Message="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                Severity="{x:Bind ViewModel.InfoBarState, Mode=OneWay, Converter={StaticResource InfoBarSeverityConverter}}"
                IsOpen="{x:Bind ViewModel.InfoBarState, Mode=OneWay, Converter={StaticResource InfoBarVisibilityConverter}}"
                IsClosable="True">
                <InfoBar.ActionButton>
                    <Button
                        Content="切换至 AAC"
                        Command="{x:Bind ViewModel.FallbackToAacCommand}"
                        Visibility="{x:Bind ViewModel.ShowFallbackButton, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                </InfoBar.ActionButton>
            </InfoBar>

            <!-- Device Selection Card -->
            <Grid
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <FontIcon
                        Grid.Column="0"
                        Glyph="&#xE702;"
                        FontSize="20"
                        Margin="0,0,12,0"
                        Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                    <TextBlock
                        Grid.Column="1"
                        Text="蓝牙音频设备"
                        Style="{StaticResource SubtitleTextBlockStyle}"
                        VerticalAlignment="Center" />
                    <Button
                        Grid.Column="2"
                        Command="{x:Bind ViewModel.RefreshDevicesCommand}"
                        ToolTipService.ToolTip="刷新设备列表"
                        Style="{StaticResource SubtleButtonStyle}">
                        <FontIcon Glyph="&#xE72C;" FontSize="14" />
                    </Button>
                </Grid>

                <ProgressBar
                    Grid.Row="1"
                    IsIndeterminate="True"
                    Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                    Margin="0,0,0,8" />

                <ComboBox
                    Grid.Row="2"
                    x:Name="DeviceSelector"
                    HorizontalAlignment="Stretch"
                    PlaceholderText="请选择蓝牙音频设备"
                    ItemsSource="{x:Bind ViewModel.Devices, Mode=OneWay}"
                    SelectedItem="{x:Bind ViewModel.SelectedDevice, Mode=TwoWay}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Padding="4" ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <FontIcon
                                    Grid.Column="0"
                                    Glyph="&#xE7F7;"
                                    FontSize="20"
                                    VerticalAlignment="Center" />

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock
                                        Text="{Binding Name}"
                                        Style="{StaticResource BodyStrongTextBlockStyle}" />
                                    <TextBlock
                                        Text="{Binding FormattedAddress}"
                                        Style="{StaticResource CaptionTextBlockStyle}"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                </StackPanel>

                                <Border
                                    Grid.Column="2"
                                    VerticalAlignment="Center"
                                    Padding="8,4"
                                    CornerRadius="4"
                                    Background="{ThemeResource SystemFillColorSuccessBackgroundBrush}"
                                    Visibility="{Binding IsConnected, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <TextBlock
                                        Text="{Binding ConnectionStatusDisplay}"
                                        Style="{StaticResource CaptionTextBlockStyle}"
                                        Foreground="{ThemeResource SystemFillColorSuccessBrush}" />
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </Grid>

            <!-- Device Info Card -->
            <Grid
                Visibility="{x:Bind ViewModel.SelectedDevice, Mode=OneWay, Converter={StaticResource NullToVisibilityConverter}}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock
                        Text="当前编码"
                        Style="{StaticResource BodyTextBlockStyle}"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <TextBlock
                        Text="{x:Bind ViewModel.SelectedDevice.ActiveCodecDisplay, Mode=OneWay}"
                        Style="{StaticResource SubtitleTextBlockStyle}"
                        Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                </StackPanel>

                <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                    <TextBlock
                        Text="连接状态"
                        Style="{StaticResource BodyTextBlockStyle}"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <TextBlock
                        Text="{x:Bind ViewModel.SelectedDevice.ConnectionStatusDisplay, Mode=OneWay}"
                        Style="{StaticResource SubtitleTextBlockStyle}" />
                </StackPanel>
            </Grid>

            <!-- Codec Selection Card -->
            <Grid
                Visibility="{x:Bind ViewModel.SelectedDevice, Mode=OneWay, Converter={StaticResource NullToVisibilityConverter}}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <FontIcon
                        Grid.Column="0"
                        Glyph="&#xE8D6;"
                        FontSize="20"
                        Margin="0,0,12,0"
                        Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                    <TextBlock
                        Grid.Column="1"
                        Text="选择音频编码"
                        Style="{StaticResource SubtitleTextBlockStyle}"
                        VerticalAlignment="Center" />
                </Grid>

                <ItemsRepeater
                    Grid.Row="1"
                    ItemsSource="{x:Bind ViewModel.CodecOptions, Mode=OneWay}">
                    <ItemsRepeater.Layout>
                        <StackLayout Spacing="8" />
                    </ItemsRepeater.Layout>
                    <ItemsRepeater.ItemTemplate>
                        <DataTemplate x:DataType="vm:CodecOption">
                            <Grid
                                Padding="16"
                                CornerRadius="6"
                                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                BorderThickness="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <RadioButton
                                    Grid.Column="0"
                                    GroupName="CodecSelection"
                                    IsChecked="{x:Bind IsActive, Mode=OneWay}"
                                    IsEnabled="{x:Bind IsEnabled, Mode=OneWay}"
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center"
                                    MinWidth="0" />

                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock
                                            Text="{x:Bind CodecInfo.DisplayName, Mode=OneWay}"
                                            Style="{StaticResource BodyStrongTextBlockStyle}" />

                                        <Border
                                            Padding="8,2"
                                            CornerRadius="4"
                                            Background="{ThemeResource SystemFillColorSuccessBackgroundBrush}"
                                            Visibility="{x:Bind IsActive, Mode=OneWay}">
                                            <TextBlock
                                                Text="使用中"
                                                FontSize="11"
                                                Foreground="{ThemeResource SystemFillColorSuccessBrush}" />
                                        </Border>

                                        <Border
                                            Padding="8,2"
                                            CornerRadius="4"
                                            Background="{ThemeResource SystemFillColorCautionBackgroundBrush}"
                                            Visibility="{x:Bind CodecInfo.IsNativelySupported, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}">
                                            <TextBlock
                                                Text="非原生"
                                                FontSize="11"
                                                Foreground="{ThemeResource SystemFillColorCautionBrush}" />
                                        </Border>
                                    </StackPanel>

                                    <TextBlock
                                        Text="{x:Bind CodecInfo.Description, Mode=OneWay}"
                                        Style="{StaticResource CaptionTextBlockStyle}"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        TextWrapping="WrapWholeWords"
                                        MaxLines="2" />
                                </StackPanel>

                                <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="12,0" HorizontalAlignment="Right">
                                    <TextBlock
                                        Text="{x:Bind CodecInfo.BitrateDisplay, Mode=OneWay}"
                                        Style="{StaticResource CaptionTextBlockStyle}"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        HorizontalAlignment="Right" />
                                    <TextBlock
                                        Text="{x:Bind CodecInfo.QualityDisplay, Mode=OneWay}"
                                        Style="{StaticResource CaptionTextBlockStyle}"
                                        Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                        HorizontalAlignment="Right" />
                                </StackPanel>

                                <Border
                                    Grid.Column="3"
                                    VerticalAlignment="Center"
                                    Padding="8,4"
                                    CornerRadius="4">
                                    <TextBlock
                                        Text="{x:Bind IsSupported, Mode=OneWay, Converter={StaticResource SupportStatusConverter}}"
                                        FontSize="12"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ItemsRepeater.ItemTemplate>
                </ItemsRepeater>

                <Grid Grid.Row="2" Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <ProgressRing
                        Grid.Column="0"
                        IsActive="{x:Bind ViewModel.IsSwitching, Mode=OneWay}"
                        Visibility="{x:Bind ViewModel.IsSwitching, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                        Width="20"
                        Height="20"
                        HorizontalAlignment="Left" />

                    <Button
                        Grid.Column="1"
                        Content="应用编码"
                        Style="{StaticResource AccentButtonStyle}"
                        Command="{x:Bind ViewModel.SwitchCodecCommand}"
                        MinWidth="120" />
                </Grid>
            </Grid>

            <!-- LDAC Info Card -->
            <Grid
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <FontIcon
                    Grid.Column="0"
                    Glyph="&#xE946;"
                    FontSize="20"
                    Margin="0,0,12,0"
                    Foreground="{ThemeResource SystemFillColorCautionBrush}"
                    VerticalAlignment="Top" />

                <StackPanel Grid.Column="1" Spacing="4">
                    <TextBlock
                        Text="关于 LDAC 支持"
                        Style="{StaticResource BodyStrongTextBlockStyle}" />
                    <TextBlock
                        TextWrapping="WrapWholeWords"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                        <Run Text="LDAC 是 Sony 开发的高解析度蓝牙音频编码，Windows 11 尚未原生支持。本应用通过修改系统蓝牙 A2DP 参数来尝试启用 LDAC 编码。" />
                        <LineBreak />
                        <Run Text="若设备不支持所选编码，将自动建议回退至 AAC。切换编码需要管理员权限和蓝牙连接重启。" />
                    </TextBlock>
                </StackPanel>
            </Grid>

        </StackPanel>
    </ScrollViewer>
</Page>
